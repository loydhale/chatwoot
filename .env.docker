# Docker-specific environment overrides
# These values override .env when running via docker-compose
#
# The docker-compose.yaml uses env_file: .env and then these
# environment variables override via the `environment:` block.
#
# If you're using docker-compose, update your .env to use these values:

# Database points to docker postgres service
POSTGRES_HOST=postgres
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=
POSTGRES_DATABASE=deskflows

# Redis points to docker redis service
REDIS_URL=redis://redis:6379

# Frontend URL for Docker
FRONTEND_URL=http://0.0.0.0:3000

# Development mode
RAILS_ENV=development
NODE_ENV=development
